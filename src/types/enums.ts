export enum ButtonTypeEnum {
  SUBMIT = "submit",
  RESET = "reset",
  BUTTON = "button",
}

export enum InputTypeEnum {
  TEXT = 'text',
  CHECKBOX = 'checkbox',
  NUMBER = 'number', 
  DATE = 'date', 
  EMAIL = 'email', 
  PASSWORD = 'password', 
  RADIO = 'radio', 
  SELECT = 'select',
}

export enum TodoListModeEnum {
  ACTIVE = 'active',
  DELETED = 'deleted',
}